export enum TypeNotification {
    COMMENT = 'comment',
    GROUP = 'group',
    PROJECT = 'project',
    ORDER = 'order'
}

export enum StatusNotification {
    FAILED = 0,
    SUCCESS = 1
}

export enum ReadNotification {
    UNREAD = 0,
    READ = 1
}